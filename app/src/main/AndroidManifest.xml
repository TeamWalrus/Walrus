<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bugfuzz.android.projectwalrus">

    <uses-feature android:name="android.hardware.usb.host" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <service
            android:name=".CardDeviceService"
            android:exported="false">
        </service>

        <activity
            android:name=".DebugActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <activity android:name=".MyWalletActivity"
                  android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailedCardViewActivity"
            android:label="Card Details"
            android:parentActivityName=".MyWalletActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".TerminalActivity"
            android:label="Terminal"
            android:parentActivityName=".MyWalletActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".AddCardActivity"
            android:label="Add Card"
            android:parentActivityName=".MyWalletActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".EditCardActivity"
            android:label="Edit Card"
            android:parentActivityName=".DetailedCardViewActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>
