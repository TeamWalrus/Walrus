<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.bugfuzz.android.projectwalrus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyWalletActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".DisplayDetailedCardviewActivity"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                  android:label=" "
                  android:parentActivityName="com.bugfuzz.android.projectwalrus.MyWalletActivity" >

        </activity>
    </application>

</manifest>